<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /trgovina/

    # Preusmjerenje za admin folder
    RewriteCond %{REQUEST_URI} !^/trgovina/admin/
    RewriteRule ^(.*)$ /trgovina/$1 [L]
</IfModule>
